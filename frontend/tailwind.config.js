module.exports = {
  darkMode: 'class', // Ustawienia domyślne dla trybu ciemnego
  content: ['./src/**/*.{js,jsx,ts,tsx}'], // Twój folder źródłowy
  theme: {
    extend: {
      colors: {
        male: "#a4ecff",
        female: "#fdaed8",
      },
    },
  },
  plugins: [],
}
